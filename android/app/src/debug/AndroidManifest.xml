<?xml version="1.0" encoding="utf-8"?>
<!-- Build debug uniquement : autorise le HTTP en clair vers un backend local
     (émulateur -> 10.0.2.2) et fait confiance à la CA mkcert pour le HTTPS de
     dev (https://id.mindlog.localhost). La prod (release) reste HTTPS strict. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:usesCleartextTraffic,android:networkSecurityConfig" />
</manifest>
