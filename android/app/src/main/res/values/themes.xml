<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème système minimal : Compose (MindlogTheme) prend le relais pour les couleurs. -->
    <style name="Theme.Mindlog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
